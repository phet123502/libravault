version: 0.0
os: linux

files:
  - source: library_backend
    destination: /home/ubuntu/libravault/library_backend
  - source: library_frontend/dist
    destination: /home/ubuntu/libravault/library_frontend/dist

permissions:
  - object: /home/ubuntu/libravault
    owner: ubuntu
    group: ubuntu
    mode: 755
    type:
      - directory
  - object: /home/ubuntu/libravault
    owner: ubuntu
    group: ubuntu
    mode: 644
    type:
      - file

hooks:
  BeforeInstall:
    - location: scripts/before_install.sh
      timeout: 60
      runas: root

  AfterInstall:
    - location: scripts/after_install.sh
      timeout: 300
      runas: root

  ApplicationStart:
    - location: scripts/start_server.sh
      timeout: 60
      runas: root
