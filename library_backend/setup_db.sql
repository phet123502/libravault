-- Run this in XAMPP phpMyAdmin or via XAMPP MySQL shell
-- XAMPP default: root user with no password

CREATE DATABASE IF NOT EXISTS libravault_db
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
